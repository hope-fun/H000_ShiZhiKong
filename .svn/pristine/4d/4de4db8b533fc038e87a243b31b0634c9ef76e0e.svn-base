function doSubmit() {
//	var form = document.forms[0];
//	form.action = '<p:fullurl value="/cg0501/cg050103/replyMessage/"></p:fullurl>';
//	form.submit();
	var text = $("#replyMessageTextarea").val()
	if(text.length <= 0) {
		kpyxTips($("#cg0501replyinfo").val())
		return ;
	}
	var rePelyform = $("#"+"rePelyform");
//	rePelyform.attr("action",action);
	rePelyform[0].submit();
}

function keepTextLength(content)
{
		  if (content.value.length > 20) // if too long.... trim it! 
		  	  {
		  content.value = content.value.substring(0, 20); // 
	  		  }
	

}


